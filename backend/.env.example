# Copy this file to backend/.env and fill in values.
# Never commit real API keys.

NASA_API_KEY=DEMO_KEY

# Optional — enables Gemini in AI Analyst (otherwise rule-based fallback)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
